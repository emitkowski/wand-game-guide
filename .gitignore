*.log
.DS_Store
.env
/bootstrap/cache/*.php
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
\n# SSL certs — generated locally by mkcert, never committed\ndocker/certs/*.pem

# MAP — developer-specific files (do not commit)
# Claude session state — developer specific, not shared
HANDOFF.md
.claude/settings.local.json
.setup-state

# Claude personal local rules — developer specific, not shared
CLAUDE.local.md

# Claude auto-memory — session/machine specific
# Copy *.example.md files to their non-example versions on first clone
docs/MEMORY.md
docs/memory/*.md
!docs/memory/*.example.md
!docs/memory/shared.md

# Test coverage output
coverage/

# Local-only working docs (spec drafts, write-ups) — not part of the MAP doc structure, never shared
docs/local/
